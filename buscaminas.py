class Buscaminas():
    def _init_(self, rows, cols, bombs):
        self.board = [["" for j in cols] for i in rows]
        def question(self, movs):
            mov = input("¿Qué movimiento desea realizar?" + movs)
            row = input("Ingrese fila: " + movs)
            col = input("Ingrese columna: " + movs)
            return mov, row, col
        def play(self, mov, row, col):
        def show_board(self):
        def win(self):
        def lose(self):